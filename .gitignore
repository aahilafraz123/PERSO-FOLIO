node_modules
dist
.DS_Store
*.local
.vite

# machine-local Claude settings (keep launch.json, ignore local prefs)
.claude/settings.local.json

# stray drops in the repo root (real assets live in public/)
/Screenshot*.png
/*.pdf

# working scratch — planning notes, not part of the shipped repo
plan.md

# Playwright MCP capture output
.playwright-mcp
